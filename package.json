{
  "scripts": {
   "watch": "watchify js/app.js js/helpers/* -o js/bundle.js -v && lite-server",
   "build": "browserify js/app.js js/helpers/* -o js/bundle.js && lite-server"
 }
}

/*npm run build - for production*/
/*npm run watch - for development*/
